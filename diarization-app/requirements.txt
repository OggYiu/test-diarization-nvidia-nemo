# FastAPI and server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Core libraries for diarization
numpy>=1.24
librosa>=0.10
matplotlib>=3.7
omegaconf>=2.3

# NVIDIA NeMo (ASR / diarization)
# NOTE: Install PyTorch first before installing other requirements
# For CPU:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# For CUDA (example for CUDA 11.8):
#   pip install torch --index-url https://download.pytorch.org/whl/cu118
#
# Then install NeMo:
#   pip install nemo_toolkit[all] --extra-index-url https://pypi.ngc.nvidia.com
#
# Finally install the rest:
#   pip install -r requirements.txt

